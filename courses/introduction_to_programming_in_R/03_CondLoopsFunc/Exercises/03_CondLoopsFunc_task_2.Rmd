**Question 2:** Create a function `IsInc()` that returns TRUE if the elements of the input vector `x` (of length>=2) are strictly increasing.
e.g. for the input `x <- c(-3, 0, 5)` function should return TRUE
for the input `x <- c(1, 2, 0)` function should return FALSE

```{r}
# YOUR CODE HERE
```
